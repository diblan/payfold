spring:
  application:
    name: renewal-producer
  datasource:
    url: jdbc:postgresql://localhost:5432/yourdb
    username: youruser
    password: yourpassword
  jackson:
    time-zone: Europe/Brussels
  rabbitmq:
    publisher-confirm-type: correlated
    publisher-returns: true
    template:
      mandatory: true
    cache:
      channel:
        size: 100   # kept = app.publishInFlightLimit so a parked confirm channel re-enters the cache and is reused, not churned
  batch:
    jdbc:
      initialize-schema: never


app:
  timezone: Europe/Brussels
  scanPageSize: 10000
  publishPageSize: 10000
  publishInFlightLimit: 100   # max unconfirmed sends in flight per publish page; each holds one broker channel while its confirm is pending
  confirmTimeoutMs: 10000
  scheduleCron: "0 0 3 * * *"   # 03:00 local by default


rabbitmq:
  exchange: billing.renewals
  routingKey: renewal.requested

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,renewal-job
  endpoint:
    health:
      show-details: never
