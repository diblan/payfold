# CLAUDE.md

Read `AGENTS.md` first — it is the canonical agent guide for this repo and everything
in it applies to Claude Code sessions.

The two rules most often broken: execute exactly **one** item from `docs/roadmap.md`
per session (no drive-by fixes), and run `scripts/verify.sh` before declaring done.
